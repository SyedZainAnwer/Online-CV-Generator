export type BtnVariants = 'default' | 'outline';

export interface PersonalDetails {
    name: string;
    role: string;
    email: string;
    number: string;
    location: string;
    linkedIn: string;
    description: string
}